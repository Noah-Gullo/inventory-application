<!DOCTYPE html>

<html>
    <head>
        <title>Inventory Application</title>
        <link rel="stylesheet" href="/styles.css">
    </head>

    <body>
        <% genres.forEach((genre) => { %>
            <p> Genre: <%= genre.name %>
        <% }) %>
    </body>
</html>